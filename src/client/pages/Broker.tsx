import React from "react";
import { IonContent, IonPage, IonTitle } from "@ionic/react";

const Broker: React.FC = () => {
  return (
    <IonPage>
      <IonContent>
        <IonTitle>Hello, this is the Broker page.</IonTitle>
      </IonContent>
    </IonPage>
  );
};

export default Broker;
